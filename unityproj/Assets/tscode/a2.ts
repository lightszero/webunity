class _a2
{
    //固定名称_wi_export,有此静态对象表示为一个组件
    static _wi_export = { "a1": "int", "a2": "string" };

    constructor()//尽量不要写构造器，成员初始化会由自动系统接手
    {
    }
    a1: number;
    a2: string;
    start()
    {
    }

    update()
    {
    }
}